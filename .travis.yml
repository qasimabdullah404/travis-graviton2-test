language: php
os:
  - linux
dist: focal
arch:
  - arm64-graviton2
virt: lxd
group: edge
php:
  - "7.4"
jdk:
  - oraclejdk8
services:
  - mysql
before_script:
  - composer self-update 2.0.14
  - php --version
  - sudo apt-get install -y php-common
  - sudo phpdismod -v php7.4 xdebug
  - mysql -u root -p '' -e 'drop user if exists drupal@localhost;'
  - mysql -u root -p '' -e 'create user drupal@localhost;'
  - mysql -u root -p '' -e 'drop database if exists drupal;'
  - mysql -u root -p '' -e 'create database drupal;'
  - mysql -u root -p '' -e 'grant all on *.* to drupal@localhost;' || true
script: true